# YAGNI/simplification worklist (short-lived; final commit deletes this file)

Working checklist from a verified simplification audit of branch `attempt-1`. Every item was adversarially verified; citations are exact as of audit time (re-locate if lines drift as earlier commits land).
Baseline: 308 Rust tests pass + 1 ignored; frontend tsc+vite build clean, 14 node tests pass.
Verify after every commit: `export PATH="$HOME/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH" && cargo test --workspace` and `cd apps/desktop && npm run build && npm test`.

## ⚠️ Owner decisions — RESOLVED (evidence-based, 2026-07-30)

1. **Keychain→encrypted-file token migration**: APPROVED for deletion. Verified on the sole install: `~/Library/Application Support/com.rianjs.retune/tokens.enc` exists (migration already ran), the live `token-file-key` keychain entry is present, and no legacy keychain token item remains. Worst case for any other stale install: one Spotify re-auth.
2. **Pre-`browserVisible` export files**: APPROVED for deletion. Restoring an ancient export gives default pane visibility instead of inferred — cosmetic, one-time, no data loss.
3. Store.rs settings/cooldown migrations need **no** further gate: the verifier empirically checked `~/Library/Application Support/com.rianjs.retune/` — settings.json and cooldowns.json are already in current shape, and settings re-save on every launch. But note: a wrong call there fails app **startup** (setup() propagates load errors with `?`), not a soft error.

---

## Commit 1 — `refactor: remove dead code and unused api surface`

Pure deletions, no behavior change anywhere. Est. −190 LOC. Risk: low.

- [x] Delete `apps/desktop/src-tauri/src/local_spike.rs` (118 lines, debug-only librespot proof-of-concept superseded by production `playback/local.rs`). Remove its three lib.rs references, each with its `#[cfg(debug_assertions)]` attribute line: mod decl (lib.rs:3-4), menu item (lib.rs:1789-1790), menu-event arm (lib.rs:1848-1849). The Debug submenu disappears entirely (spike is its only entry). No TS references exist. Verify with `cargo check` in both debug and release profiles.
- [x] Delete dead `pub async fn refresh` in `crates/retune-spotify/src/auth.rs:108-122`. Zero callers (production refresh is `SpotifyClient::refresh_token`, client.rs:786). Keep `token_request` (still used by `exchange_code`).
- [x] Delete unconstructed variant `Error::MissingRefreshToken` in `crates/retune-spotify/src/lib.rs:52-53` (sole grep match is the definition; client.rs:817 falls back via `unwrap_or(stored.refresh)`).
- [x] Delete `pub type AlbumTrack = Track;` (`crates/retune-spotify/src/client.rs:1103`); declare `Album.tracks` (client.rs:1141) as `Option<Page<Track>>`. Only two occurrences repo-wide.
- [x] Delete `probe_duration` (`crates/retune-audio/src/lib.rs:245-248`), the `AudioError::DurationUnavailable` variant (lib.rs:41-43), and its only test `duration_helper_reports_container_duration` plus import (`crates/retune-audio/tests/decode.rs:3,61-65`). Duration stays covered by `all_supported_formats_probe_and_decode_to_eof`.
- [x] Remove `Serialize, Deserialize` derives and the seven `#[serde(default, skip_serializing_if)]` attributes from `NewTrack` (`crates/retune-core/src/model.rs:402-422`). Nothing serializes it (no tauri command takes it; all containing structs are non-serde). Verifier already ran `cargo check --workspace --all-targets` clean with this change.
- [x] Delete the impossible-case `reject_local_uris` call in `add_spotify_album` (`apps/desktop/src-tauri/src/spotify_commands.rs:244-249`). Track URIs come from Spotify catalog album objects, which never contain `file:` URIs. Keep the `uris` vec (feeds `save_to_spotify` at :250) and keep the album-URI guard at :221.
- [x] Delete the dead type re-export at `apps/desktop/src/App.tsx:12` (`export type { AlbumPageView, ... } from './types.ts'`) — all consumers import from `./types.ts` directly.
- [x] Remove never-read mirrored fields `ConnectionState.missing_scopes` (`apps/desktop/src/types.ts:37`, init at App.tsx:128) and `PlayerState.volumeSupported` (types.ts:136, set at App.tsx:171). Rust keeps sending them; extra JSON fields are ignored. (Owner aside: if volumeSupported was meant to gate the volume slider for Connect devices, that's a missing feature to build separately, not a reason to keep an unread field.)

## Commit 2 — `refactor: retire dead gzip path in core and simplify retune-core model`

Est. −70 LOC. Risk: low.

- [x] Retire core gzip: delete `export_json_gz` (`crates/retune-core/src/io.rs:45-51`, only caller is its own test at io.rs:133), delete the gzip-sniff branch of `import` (io.rs:57-65) so it takes plain JSON, delete `ImportError::Gzip` (never matched outside io.rs), drop `flate2` from `crates/retune-core/Cargo.toml:10`, and update/delete the gzip tests (io.rs:133, 146-149). Safe for old installs: store save always wrote plain `export_json`; the user Restore/Merge path decompresses in the shell (lib.rs:1937-1943) before core import runs; verifier confirmed no historical writer of a gzipped library.json ever existed. `flate2` correctly stays in `apps/desktop/src-tauri/Cargo.toml` (shell does its own gzip on import). Coordinate the io.rs module doc with Commit 12.
- [x] Drop the always-false `compressed` parameter from `export_library` (`apps/desktop/src-tauri/src/lib.rs:1863-1889`; sole caller is menu handler at :1802 passing `false`) AND from `export_with_settings` — the gzip encoder there is only exercised by the round-trip test at lib.rs:3259. Have that test gzip the plain output itself so the shell's import GzDecoder path (lib.rs:1937-1940, which must stay — the import filter accepts .gz) remains covered.
- [x] Add private `fn track_mut(&mut self, id: TrackId) -> Result<&mut TrackRecord, UnknownTrack>` to `impl Library` and replace the four verbatim find-or-UnknownTrack blocks in `edit` (model.rs:227-231), `click_track_star` (255-259), `set_track_rating` (271-275), `set_track_enabled` (281-286). Mirrors existing `get()` at model.rs:163. −12 LOC.
- [x] Simplify id bookkeeping (use the verifier's revised form, NOT the original `.expect` variant — importing id u64::MAX must stay an `Err`, not a panic): in `validate_imported` replace the try_fold (model.rs:373-383) with `let past_max = match self.tracks.iter().map(|track| track.id.0).max() { None => 0, Some(max) => max.checked_add(1).ok_or_else(|| format!("track id {max} exhausts the id space"))?, };`. In `fresh_id` (model.rs:390-397) delete the while-loop collision scan (unreachable — next_id is recomputed past all ids on every construction path), keeping the single `checked_add(1).expect(...)`. The io.rs hostile-next_id and duplicate-id tests pin surviving behavior.
- [x] Move the `UNCATEGORIZED` sentinel to core: define `pub const UNCATEGORIZED: &str = "Uncategorized";` in `crates/retune-core/src/model.rs` (re-export from lib.rs:12), use it in `crates/retune-core/src/browse.rs:67` (currently a bare literal that pins sort order), and change `crates/retune-spotify/src/normalize.rs:7` to `pub use retune_core::UNCATEGORIZED;` so the four shell imports keep compiling. Leave the TS literals (ui.ts:26, dialogViews.tsx:34/42/68) alone — no shared Rust/TS constant mechanism exists and inventing one would be speculative. +1 LOC.

## Commit 3 — `refactor: retire one-time migration and legacy-compat code` ⚠️

Owner-gated items marked below; if the owner defers a gate, land the ungated items alone. Est. −330 LOC across the three. Risk: medium (old-install compat).

- [x] Retire store.rs legacy migrations (note: `settings_load_heals_legacy_column_order` and `visual_settings_apply_normalizes_legacy_columns` fed inputs that healed into the exact OLD_COLUMNS order and so pinned the deleted upgrade branch; their column-order expectations were updated to the surviving generic-append result rather than deleting them) (verified safe now — installed files already current-shape; see header note 3). Delete: `OLD_COLUMNS` const + its exact-match upgrade branch (`apps/desktop/src-tauri/src/store.rs:246-248, 267-280`); the legacy `browserVisible` inference block in `FsSettingsStore::load` (store.rs:499-508); the manual `Deserialize` impl for `Cooldown` accepting bare numbers (store.rs:411-431 — replace with `#[derive(Deserialize)]` alongside the existing `serde(rename_all = "camelCase")`); the `streaming_bitrate < 160` clamp (store.rs:304-306). Delete tests: `legacy_hidden_browser_migrates_to_independent_visibility`, `legacy_visible_browser_preserves_panes`, `settings_load_normalizes_legacy_streaming_quality`, `legacy_numeric_cooldown_loads_as_transient`, `settings_load_upgrades_exact_old_default_order`. KEEP the generic append-new-columns path (281-292), unknown-column pruning, and `settings_load_heals_legacy_column_order`. Keep the insert-'track'-if-missing branch (264-266) — conservative choice; its insert-at-0 position differs from the generic append path that `..._heals_legacy_column_order` relies on.
- [x] ⚠️ (Gate 1) Retire the Keychain→encrypted-file token migration: delete `KeychainTokenStore` (`crates/retune-spotify/src/tokens.rs:125-162`), `LEGACY_ACCOUNT` (tokens.rs:20), `migrate_token_store` (tokens.rs:305-324), its private helper `file_exists` (tokens.rs:239-249, migration-only), the three migration tests (tokens.rs:630-685), and the release-startup migration block + imports (`apps/desktop/src-tauri/src/lib.rs:52, 2191-2197` — release branch reduces to just `EncryptedFsTokenStore::new(...)`). `KeychainKeySource` (encryption key in Keychain) is the live mechanism and STAYS. Also KEEP `#[serde(default)]` on `Tokens.scopes` and the `legacy_record`/`legacy_grant` tests — they also cover pre-scopes records in the dev plaintext `FsTokenStore`, not just keychain compat.
- [x] ⚠️ (Gate 2) Retire legacy export-format shims: delete the pre-browserVisible backfill block in `import_with_settings` (`apps/desktop/src-tauri/src/lib.rs:1950-1965` — derives browserVisible from browserPanes and resets panes) and the four legacy-default tests at lib.rs:3290-3331 (`legacy_visual_settings_default_all_browser_panes_visible`, `legacy_visual_settings_default_playlists_expanded`, `legacy_export_defaults_shuffle_off...`, `legacy_visual_settings_default_column_widths_to_empty`), keeping the modern round-trip test. Serde defaults on the struct already handle missing fields. Decide this BEFORE the ExportSettings merge in Commit 7 — if this lands first, those four tests are deleted instead of rewritten.

## Commit 4 — `refactor: simplify retune-spotify internals`

Sequence after Commit 3: the keychain deletion removes 6 of the 12 `token_error` sites. Est. −50 LOC. Risk: low.

- [x] Derive `SCOPES` from `REQUIRED_SCOPES`: replace the hand-joined string const (`crates/retune-spotify/src/auth.rs:28`) with `pub static SCOPES: LazyLock<String> = LazyLock::new(|| REQUIRED_SCOPES.join(" "));` (`std::sync::LazyLock`). Call-site fixes: `&SCOPES` in `authorize_url` (auth.rs:72); `auth::SCOPES.clone()` at `apps/desktop/src-tauri/src/lib.rs:2559`, lib.rs:3109, AND `spotify_commands.rs:58` (currently `.into()`, which won't compile against the static); prefix `&` on the ~17 `tokens(retune_spotify::auth::SCOPES)` calls in playlists.rs tests (deref coercion handles the rest — but Commit 11 rewrites those sites anyway; coordinate). Delete the now-pointless sync-guard test `scopes_request_string_matches_required_list` (tokens.rs:524-527).
- [x] Consolidate optional-device_id query building: replace `device_path` (`crates/retune-spotify/src/client.rs:898-908`) with one sync helper `fn player_path(path: &str, pairs: &[(&str, &str)], device_id: Option<&str>) -> String` (returns path unchanged when both empty) and use it in `set_repeat` (client.rs:562-569), `set_volume` (597-604), `seek` (612-620, deleting its Serializer/Send-scoping comment — the helper contains the Serializer entirely), and play/resume/pause (empty pairs). Tests at client.rs:1732-1766 assert exact request URLs incl. percent-encoding. −20 LOC.
- [x] Use the existing `token_error` helper (tokens.rs:348-350) at the remaining inline sites: `.map_err(|error| Error::TokenStore(error.to_string()))` → `.map_err(token_error)`; match arms `Err(error) => Err(Error::TokenStore(error.to_string()))` → `Err(error) => Err(token_error(error))`. Sites (pre-keychain-deletion numbering): tokens.rs:97, 111, 120 (CachedTokenStore), 366, 374, 381 (InMemoryTokenStore), plus the KeychainTokenStore sites if Gate 1 was deferred. Do NOT move the fn — Rust item order is irrelevant.
- [x] Replace poisoned-mutex→`Error::Transport` mislabels with the codebase's expect style: `.lock().expect("artist cache mutex poisoned")` in `artist()` (client.rs:423-425, 432-434) and same for `request_counts` in `send_api_request` (client.rs:759-762), matching the existing expects at client.rs:196-206. Poisoning is unreachable (nothing panics holding these locks).

## Commit 5 — `refactor: simplify local-file import path`

Est. −37 LOC. Risk: low. Note the bitrate item is a small deliberate behavior change (consistency fix).

- [x] Consolidate epoch-seconds: make `unix_now()` at `apps/desktop/src-tauri/src/lib.rs:1225-1230` `pub(crate)`; delete the byte-identical copy at `provider.rs:280-285` (provider tests keep compiling via `use crate::unix_now;`); replace the inline expansions at `localfiles.rs:112-117`, `localfiles.rs:131-134`, and `spotify_commands.rs:53-56` (dropping their expect/map_err impossible-case handling in favor of unix_now's `unwrap_or_default`). Drop the then-unused `SystemTime`/`UNIX_EPOCH` imports in localfiles.rs (line 4) and spotify_commands.rs. (The fourth copy in retune-spotify client.rs:841 stays — separate crate, don't couple.)
- [x] Narrow `scan_paths(&[PathBuf])` to `scan_path(path: &Path) -> Vec<PathBuf>` (`crates/retune-audio/src/import.rs:22-31`): every caller passes exactly one path. Keep recursive scan, sort, canonicalize/BTreeSet dedup (needed for symlink aliases within one root); drop the outer loop/per-root Vec/cross-root merge. Rename the re-export in `crates/retune-audio/src/lib.rs:5`. Callers: `localfiles.rs:48` becomes `scan_path(supplied)` (drops a clone); test sites `crates/retune-audio/tests/import.rs:28, 58, 61, 62` drop their slice wrapping.
- [x] Make `map_file` infallible (`apps/desktop/src-tauri/src/localfiles.rs:83-101`): the sole error is the `ok_or_else` at :101, unreachable (canonicalized, symphonia-probed file always has a file stem). Return `NewTrack`, using `.expect("canonical file path has a file stem")` on the file_stem fallback (verifier prefers expect over `unwrap_or_default` so an impossibility fails loudly). Call site (localfiles.rs:53-56) becomes `import_file(&path).map(map_file).map_err(|e| e.to_string())` (map before map_err); delete the six test `.unwrap()`s at localfiles.rs:234, 250, 256, 258, 371, 377, 410.
- [x] Fix bitrate-source inconsistency: in `map_file`, compute `bitrate_kbps` from `file.tags.duration` (the value actually stored on the track, matching `backfill_metadata` at localfiles.rs:160) instead of `info.duration` (localfiles.rs:85-91): `std::fs::metadata(&file.canonical_path).ok().and_then(|m| average_bitrate(m.len(), file.tags.duration))`. Removes map_file's dependency on info.duration (then used only for codec). Effect: raw .aac imports get a bitrate at import instead of on next-startup backfill. Existing mp3 bitrate-range test (localfiles.rs:244-246) still passes; no test pins the old aac None-at-import behavior.

## Commit 6 — `refactor: consolidate playlist persistence and merge twin error enums`

Merged from two overlapping findings. Est. −145 LOC. Risk: medium (command fns untested — they need an AppHandle — so changes are mechanical but unguarded; keep them literal).

- [x] Add `fn save_playlists(app: &tauri::AppHandle, cache: playlists::PlaylistCache) -> Result<(), String>` next to `mutate_library` (lib.rs:1536) doing save → mutex swap → `playlists-changed` emit. Replace all 10 verbatim sites: `apps/desktop/src-tauri/src/lib.rs:1195-1201`, lib.rs:1527-1533 (playlist_add_inner), lib.rs:2040-2047 (apply_import — near-duplicate: use `if let Err(e) = save_playlists(&app, playlists) { notify_error(app, e); return; }`; only delta is an in-process emit failure would now notify and skip the library-changed emit — practically impossible), and `playlist_commands.rs:47-53, 68-74, 146-152, 211-217, 221-227, 256-262, 266-272` (playlist_reorder and playlist_remove each contain it twice, Ok arm + Reloaded arm — both arms collapse to `save_playlists(...)?` then `Ok(())` or `Err(playlists::STALE_PLAYLIST.into())`).
- [x] Merge the structurally identical enums `PlaylistReorderError` (`apps/desktop/src-tauri/src/playlists.rs:323-328`) and `PlaylistRemoveError` (playlists.rs:414-425) into one `PlaylistMutationError { Reloaded, Spotify(Error), Other(String) }` (keep the single `From<Error>` impl). Replace the two byte-identical 24-line match blocks in playlist_commands.rs (209-232 vs 254-277) with one shared handler. ~28 test references need a mechanical rename; `stale_reorder_refreshes_tracks_and_reports_reload` / `stale_remove_refreshes_tracks_and_reports_reload` pin behavior.
- [x] (Weakest leg — only after the enum merge makes the blocks literally identical; skip if it needs awkward parameter threading.) Extract the duplicated 400|409 stale-snapshot recovery block in `playlists::reorder` (playlists.rs:293-307) and `playlists::remove` (389-400) into a small helper.

## Commit 7 — `refactor: simplify shell sync, export, and settings plumbing`

Est. −105 LOC. Risk: low-medium. Sequence after Commit 3 (ExportSettings item interacts with Gate 2; column consts compound with the OLD_COLUMNS deletion).

- [x] Replace the provider rebuild in `sync_spotify_inner` (`apps/desktop/src-tauri/src/lib.rs:1015-1025`) with `let provider = provider_from(&state)?;` — deleting the settings lock, `spotify_provider` call, and mutex write-back. Verified invariant: `state.spotify` mirrors the persisted client id at all three mutation points (startup lib.rs:2216, set_settings lib.rs:722-725, connect_spotify spotify_commands.rs:68-69); the error string at lib.rs:1023 is byte-identical to provider_from's (lib.rs:829). Untested path (needs AppHandle) — hence medium risk; behaviorally equivalent by construction.
- [x] Extract cooldown persistence in `artist_albums_outcome` (`apps/desktop/src-tauri/src/lib.rs:1346-1424`): `fn record_cooldown(sync_store: &FsSyncStore, endpoint: &str, kind: store::CooldownKind, deadline: u64, now: u64) -> Result<(), String>` (load cooldowns, insert at endpoint_family, save), called from both the RateLimited arm (1376-1389) and Quota arm (1400-1413) — identical except CooldownKind. Do NOT extract a shared message function: the pre-check's "quota is STILL exhausted" vs the live arm's "quota is exhausted" wording is intentional and test-pinned (lib.rs:2596, 2614-2615). Keep all four message strings inline. Tests at lib.rs:2577-2637 verify. Net ~−14. (This is also the full surviving scope of the cross-cutting "outage handling written three times" finding — provider.rs's snapshot_page and GenreSource::artist arms differ semantically and stay as-is.)
- [x] Extract the 8-way partial-import message matrix from `sync_spotify_inner` (`apps/desktop/src-tauri/src/lib.rs:1090-1140`): `fn partial_import_message(detail: &str, quota_exhausted: bool, earliest_cooldown: Option<u64>, now: chrono::DateTime<chrono::Local>) -> String` preserving every string byte-for-byte, plus a `const GENRES_DEGRADED_MSG: &str` for the warning duplicated verbatim at lib.rs:1129 and 1136. Add a compact unit test over the variants (none of these strings appear in any test today). Roughly LOC-flat; sync_spotify_inner shrinks ~50 lines.
- [x] ⚠️ UNVERIFIED (auditor grep-verified, no adversarial pass — re-verify claims during implementation): merge `VisualSettings`/`BehavioralSettings`/`ExportSettings` (`apps/desktop/src-tauri/src/lib.rs:149-233`) into one `ExportSettings` with all 13 fields (`#[serde(default)]` on `shuffle`), one `from_settings`, one `apply_to` (keeps `settings.normalize()`, sets `settings.shuffle`). Both sub-structs are serde-flattened, so serialized JSON is byte-identical; neither is used independently outside tests. Update tests constructing VisualSettings JSON (lib.rs:3290-3387) — note Commit 3 Gate 2 may have already deleted four of them. Confirm the legacy-default assertion at lib.rs:3311 still holds. −45 LOC.
- [x] Deduplicate store.rs column lists: add `const OPTIONAL_COLUMNS: [&'static str; 6] = ["disc", "kind", "bitrate", "lastPlayed", "added", "releaseDate"];` next to `Settings::COLUMNS` (`apps/desktop/src-tauri/src/store.rs:230-245`). `Default::default()` uses `Self::COLUMNS.map(String::from).to_vec()` for column_order (currently spelled out at 181-198) and `Self::OPTIONAL_COLUMNS...` for hidden_columns (200-209); replace the `matches!` arm (284-287) with `Self::OPTIONAL_COLUMNS.contains(&column)`. (The third copy in the OLD_COLUMNS branch is already gone via Commit 3.) Existing settings tests pin exact resulting orders. −30 LOC.

## Commit 8 — `refactor: narrow media provider trait and dedupe snapshot sync`

Est. −90 LOC. Risk: low-medium. Add the new test BEFORE the Shows/Audiobooks refactor.

- [x] Shrink `MediaProvider` (`apps/desktop/src-tauri/src/provider.rs:134-149`) to the sync surface actually used by its only generic consumer `sync::snapshot` (sync.rs:36-67): `library_snapshot`, `earliest_cooldown`, `request_counts`. Move `search`/`album_tracks` to free functions in provider.rs taking `&SpotifyClient` (the pattern `artist_albums_page` at provider.rs:1004 already uses); call sites change to `provider::search(provider.as_ref(), ...)` (spotify_commands.rs:130, 224; playlist_commands.rs:174; lib.rs:2844 test — losing the UFCS disambiguation). Delete `save_to_spotify` entirely: at its one call site (spotify_commands.rs:250) call `client.save_to_library(...).map_err(|e| e.to_string())`, matching :298 which already bypasses the wrapper. Delete the three delegation methods from SpotifySyncProvider (provider.rs:1058-1068), the three FakeProvider stubs (1106-1121), and the provider.rs:1976 wrapper test (coverage exists on the inherent client method, retune-spotify client.rs:1740, 1789). Remaining behavior pinned by `search_maps_results_and_uses_search_page_limit` and the lib.rs:2844 album_tracks test (rename only).
- [x] Extract `fn search_album(album: Album) -> SearchAlbum` next to the existing `image_url`/`album_track_count` helpers and use `.map(search_album)` at the two verbatim 18-line mapping sites: `provider.rs:928-945` (search) and 1017-1034 (artist_albums_page) — the only two SearchAlbum producers. Both sites test-covered. −18 LOC.
- [x] Dedupe the Shows/Audiobooks/Albums emit-and-bail blocks in `spotify_library_snapshot` WITHOUT a generic helper (verifier rejected the original generic `expand_containers` — closure/type-param soup for two call sites). Instead: in the Shows arm (provider.rs:762-802), Audiobooks arm (820-861), and Albums arm (728-760), add `let mut interrupted = false;` and replace each inline `on_batch(...) + batches.push + break 'pages` block (five total: 742-749, 772-781, 784-792, 830-839, 841-851) with `interrupted = true; break;` (inner loop only). Shows/Audiobooks return `(batch, count, page.next.is_some() && !interrupted)` so the existing shared tail emit at 863-871 fires once and the `!has_next` check exits the page loop; Albums falls through to its own emit at 753-759 and returns `(vec![], count, page.next.is_some() && !interrupted)`. Identical observable behavior. **First** add one test driving a rate_limited response on `show_episodes` (mirror `album_track_rate_limit_keeps_previously_collected_tracks`) — the Shows/Audiobooks partial path is currently untested. ~−30 LOC.
- [x] Document the hidden ordering invariant (verifier downgraded the code change to a comment — the proposed `finish()` trait method would silently drop flush-time degradation flags and break `sync_artist_budget_stops_at_cap...`): add a short comment on `LibraryKind::ALL` (provider.rs:34) stating Audiobooks must remain the final section because SpotifySyncProvider parks music batches in `SyncRun::pending_music` and flushes+enriches only when `kind == Audiobooks` (provider.rs:678, 879), and sync::snapshot must iterate ALL to completion. Optionally a one-line pointer at the 879 flush site.

## Commit 9 — `refactor: simplify playback controller and connect backend`

Est. −120 LOC. Risk: low (medium on the reducer item). All in `apps/desktop/src-tauri/src/playback/`.

- [x] Reduce the unreachable Local arm of `PlayerBackend::step` (mod.rs:285-312) to `backend.stop(); Ok(())`: step is only called from step_locked (mod.rs:767) after `step_index` already returned None with identical inputs, so the re-run deterministically returns None again — the whole `Some(next)` branch (reject_chapter, queue_load, backend.play) is dead. Drop the now-unused `reducer` and `wrap` parameters and the ControllerState destructuring at mod.rs:764-766; call `state.backend.step(client, direction)` directly (borrows check out).
- [ ] Extract `fn advance_generation(state: &mut ControllerState, generation: u64)` (sets state.generation, `file.set_generation`, `reducer.activate`) and use it at the 5 verbatim sites: switch_to_local_with (mod.rs:643-646 — helper must take generation as a param, this site uses `local.generation()` not wrapping_add), switch_to_connect (654-660), invalidate_local (667-671), ensure_valid (722-724), ReducerAction::Invalidate arm (843-846). Also extract the shared body of invalidate_local and the Invalidate arm into `fn invalidate_local_locked(state: &mut ControllerState)` (teardown + advance), keeping the `matches!` guard at the invalidate_local call site and the log line at the event site. Invariant-critical dedup (missing one propagation call silently breaks stale-event rejection). Realistic net ~−10. (skipped: the ReducerAction::Invalidate arm is not verbatim — it calls `reducer.recover`, not `activate` (already so at audit commit a2fb367); recover preserves resume position for reconnect, so neither advance_generation nor a shared invalidate_local_locked body applies without a behavior change)
- [x] Make `set_shuffle` delegate: drop `#[cfg(test)]` from `set_shuffle_with` (mod.rs:585-597) and make `set_shuffle` (574-583) a one-liner `self.set_shuffle_with(shuffle, |suffix| suffix.shuffle(&mut rand::rng())).await` — mirroring the play/play_with pair at 451-461. Existing shuffle tests exercise set_shuffle_with directly.
- [x] Drop `Snapshot::new`'s `shuffle` parameter (mod.rs:53-57): all 16 call sites pass `false` (production: connect.rs:154, 170, 589; tests: connect.rs:738, 788, 945, 971, 997, 1006, reducer.rs:403, mod.rs:1018, 1055, 1270, 1479, 1486, 1514). `new(tracks, index)` constructs identity order directly (no permute, no rand); keep `new_with` for play_with (real shuffle enters only via play_with/set_shuffle_with).
- [x] Strip ConnectBackend's five layers of single-use indirection (connect.rs): (a) delete passthroughs `set_live_volume`/`set_live_repeat` (402-416), making generic `set_volume`/`set_repeat_state` `pub(super)` — callers mod.rs:323/334, monomorphization inferred; (b) delete `#[cfg(test)]` `next`/`prev` wrappers (303-317) — tests (child module) call private `step_state(client, ±1)` directly; (c) delete the rename-residue alias `type Playback = ConnectBackend;` (701) and use ConnectBackend at the 4 test sites (1073, 1116, 1146, 1179); (d) gate `impl Default for ConnectBackend` (693-698) with `#[cfg(test)]` (production always uses `::new`); (e) `#[derive(Default)]` on NeutralState (mod.rs:141-152) and replace hand-rolled `empty_state()` (703-715; production-used at 278, 546) with `NeutralState::default()` — value-identical, every field is the type default.
- [x] Collapse the redundant tail of `resolve()` (connect.rs:67-70) — `if now.track_id.is_some() { return Takeover; } Takeover` — to a single trailing `PlaybackDecision::Takeover`.
- [x] Change `begin()` to take a `Snapshot` (connect.rs:143): pass it straight to `connect_snapshot` (verified safe — connect_snapshot starts with `snapshot.active_tracks()` and treats index as order-space, exactly what play() flattens today; shuffled snapshots already flow through update_snapshot), keep the returned snapshot as the context snapshot, and call play_snapshot with `&snapshot.active_tracks()` / `snapshot.index`. Deletes the flatten/rebuild at connect.rs:155-156 and 170 and the duplicate emptiness check at 150-152 (play_with validates at mod.rs:470-471). Four test call sites (1075, 1117, 1148, 1181) construct the Snapshot themselves. `start_prefers_active_desktop_and_plays_full_queue_at_offset` catches offset/order regressions.
- [x] Replace the reducer's unbounded `bindings: HashMap<u64, LoadIntent>` (reducer.rs:30, insert at 139-141) with `current: Option<(u64, LoadIntent)>`: on RequestIdChanged, `if let Some(intent) = self.pending.pop_front() { self.current = Some((request_id, intent)); }`; `current_intent` (252-257) becomes a match on stored request_id + latest_intent + uri (KEEP the latest_intent check — it preserves queued-skip rejection). `activate()` must also set `current = None` (it currently clears bindings). Only the latest-intent binding is ever acceptable (ids monotonic, pending FIFO), so behavior is identical for every covered sequence; the reducer suite (rapid_loads, repeat_one, queued_skip tests) is the gate. Risk: medium.

## Commit 10 — `refactor: simplify App.tsx event wiring and dedupe frontend plumbing`

All in `apps/desktop/src`. Est. −115 LOC. Risk: low-medium. Manual keyboard verification required for the keydown item.

- [ ] Add module-level hook `useTauriEvent<T>(event, handler)` in App.tsx using the latest-handler-ref pattern (`const ref = useRef(handler); ref.current = handler; useEffect(() => { const sub = listen<T>(event, ({ payload }) => ref.current(payload)); return () => { void sub.then((stop) => stop()) } }, [event])`). Replace the 17 hand-rolled `listen` subscriptions across 4 effects: usePlayer player-state (App.tsx:252-257), get-info (466-469), the 11-listener effect (471-496, 13 lines of cleanup), and the view-action/player-action effect (598-619, which currently re-subscribes on state changes purely for fresh closures). Must be module-level — both usePlayer and App use it. Leave `onDragDropEvent` (499) alone (different API). −45 LOC.
- [ ] Add `const fail = useCallback((error: unknown) => dispatch({ type: 'error', error: String(error) }), [])` in App and replace the ~18 verbatim `.catch((error) => dispatch({ type: 'error', error: String(error) }))` sites with `.catch(fail)`; guarded sites (404, 412, 523-528) become `.catch((error) => active && fail(error))`; the rethrow site (~767) becomes `.catch((error) => { fail(error); throw error })`. Do NOT add a `call` wrapper (duplicates usePlayer's `run`), and leave child-component `onError(String(error))` sites (1011, 1021, 1153, 1249) untouched — dispatch isn't in scope there. ~−13 LOC.
- [ ] Sync drifted defaults (fix, 2 lines): add `'disc'` to `defaultSettings.columnOrder` (after 'album') and to `hiddenColumns` at App.tsx:95/97 to match `Settings::default()` (store.rs:185, 201); add comment `// Mirrors Settings::default() in src-tauri/src/store.rs — keep in sync`. Do NOT gate rendering on settingsHydrated (verifier: blanks the app permanently if get_settings rejects).
- [ ] Make the settings-save effect fail-safe (App.tsx:434-464): `const EXCLUDED = ['repeat', 'streamingBitrate', 'normalizeVolume', 'gapless'] as const` with comment "persisted by set_repeat / set_audio_settings" (these exclusions are load-bearing — set_settings also switches playback backends, so `[state.settings]` alone is NOT safe); derive `const saveKey = useMemo(() => JSON.stringify(state.settings, (k, v) => EXCLUDED.includes(k) ? undefined : v), [state.settings])` and depend on `[saveKey, settingsHydrated, preferences]`. Replacer verified collision-safe against nested keys. New settings fields then save by default instead of silently dropping.
- [ ] Bind the 90-line keydown effect once (App.tsx:621-713, 15-entry dep list): store the handler body in a ref (same latest-ref pattern), subscribe with empty deps; delete the `typeaheadExpires` ref (346) and re-arm block (622-626) that exist only to resurrect the timer across effect re-runs, and drop the clearTimeout from teardown. Handler logic untouched. No automated coverage — manually verify typeahead, arrows, space, cmd shortcuts, Escape-in-modal.
- [ ] Change Preferences `onSave` from 11 positional params (5 booleans; dialogViews.tsx:152, forwarded positionally at 226, reassembled into Partial<Settings> at App.tsx:858-870) to a single `Pick<Settings, ...>` object built at the OK button. Keep `clientId.trim()` — it moves into the object construction as `spotifyClientId: clientId.trim()`.
- [ ] Narrow album callbacks: `onAdd: (album: { uri: string; name: string; artist: string }) => Promise<boolean>`, `onRemove: (uri: string) => Promise<boolean>` — applied consistently at SpotifyAlbumPage (spotifyViews.tsx:48-49), SpotifyArtistPage (199-200), SpotifySearch onAdd (294), and the add/remove fns (312/325). Shrinks the two fabricated 7-field SearchAlbum literals (spotifyViews.tsx:99, 108, incl. a synthesized `inLibrary` nothing reads); SearchAlbum stays structurally assignable at search-result sites. Rust commands take exactly (uri, name, artist) / (uri).
- [ ] Replace the hand-rolled mm:ss at spotifyViews.tsx:383 with `{formatTime(track.durationSecs)}` (already imported line 4, used at 154). Byte-identical under an hour.
- [ ] Extract `liveBackend()` in usePlayer — `const c = playingRef.current; return !c?.simulated && (connected || c?.uri?.startsWith('file:'))` — for the 4 drift-prone guard sites (App.tsx:296, 304, 316 negated, 323; verifier confirmed 316's early-return is the exact negation). In `start()`, collapse the two byte-identical `queue.current = playable; run('play_tracks', ...)` branches (266-270, 282-283) by reordering: spotify-and-not-connected → pending+connect; else file:/spotify: → single play_tracks branch; else → simulated dispatch. Verifier checked all four cases incl. target-not-found. `connected` already in every affected dep list.
- [ ] Delete the no-op StatusBar `+` button (App.tsx:1294 — focusable dead control, no onClick, no delegated handler) AND fix the layout it anchors: in App.css drop the `.status-bar button` rule (line 218) and change `.status-bar` grid-template-columns from `40px minmax(0,1fr) 40px` to a single centered column (e.g. `minmax(0,1fr)`; the `.status-bar > span { text-align: center }` rule already centers). Verify the sync-progress variant visually.

## Commit 11 — `refactor: consolidate test scaffolding for spotify clients and track builders`

Test-only. Sequence late — earlier commits change some of these sites. Est. −150 LOC. Risk: low.

- [ ] Add `pub fn fake_client(responses: impl IntoIterator<Item = Response>, scopes: &str) -> SpotifyClient<FakeTransport, InMemoryTokenStore>` in retune-spotify client.rs beside FakeTransport (both doubles are already pub), building never-expiring Tokens (access "access", refresh "refresh", expires_at u64::MAX). Use it at the desktop sites only: lib.rs:2546 client/playlist_client body, provider.rs:1132 client body, sync_orchestrator.rs:73 inline, connect.rs x4 inline literals (1069, 1111, 1142, 1175 at audit time), and playlists.rs's ~16 `tokens(SCOPES)` sites — leaving the deliberately token-less `InMemoryTokenStore::new(None)` site (~playlists.rs:1016, MissingToken test) untouched. Do NOT touch client.rs's own `tokens()` — its expired (expires_at 0, access "old") tokens are load-bearing for refresh-flow coverage.
- [ ] Add `Response::rate_limited(retry_after: &str)` and `Response::quota_exceeded(retry_after_secs: Option<u64>)` constructors on `impl Response` (client.rs:48). Replace the standard-shape hand-rolled builders: desktop `quota_response` (lib.rs:2564), `rate_limited`/`quota_limited` (provider.rs:1148/1154), and the standard client.rs test sites — malformed_retry_after's transient (1410-1413), concurrent_callers (1474-1475), retries_rate_limit... (1500-1501), rate_limit_retries_are_capped (1518-1522, deleting its local closure), long_rate_limit (1532-1533), quota shapes at 1383-1391. Leave hand-rolled: `unknown_and_malformed_rate_limit_bodies`' body-parameterized closure (1448-1470) and the custom quota bodies at 1428-1434.
- [ ] Add `fn stale_refresh_responses() -> [Response; 4]` in playlists.rs tests using the stale_remove variant (the one whose fourth response includes `"total": 1` — the fixtures are NOT verbatim identical); use it in both `stale_reorder_refreshes_tracks_and_reports_reload` (1032-1060) and `stale_remove_refreshes_tracks_and_reports_reload` (1184-1212); run the playlist tests to confirm the reorder deserializer tolerates the extra field (serde ignores unknown fields by default).
- [ ] Add a manual `impl Default for NewTrack` in retune-core model.rs (~14 lines; needed manually since SourceId has no Default — empty strings, SourceId::Music, Duration::ZERO, options None) and rewrite the nine 13-field test builders as struct-update literals (`..NewTrack::default()`), keeping meaningful non-None fields explicit: model.rs:457, browse.rs:151, browse.rs:333 (sets track_no/disc_no), io.rs:98 (sets kind), io.rs:185, fixture.rs:237, sync.rs:155 (sets kind), lib.rs:2379, playlists.rs:647. Don't forget `source` where a builder takes it as a parameter (browse.rs). Tests themselves are the safety net. −40 LOC.

## Commit 12 — `docs: correct stale doc comments in retune-core`

Doc-only. Depends on Commit 2 (gzip removal changes what io.rs should say).

- [ ] Rewrite `crates/retune-core/src/io.rs:53-55`: it claims import "runs version migrations so any older schema still loads" — no migrations exist; io.rs:72-79 rejects any version != 1. State actual behavior: plain-JSON, schema v1 only (post-gzip-removal), with `handwritten_v1_envelope_loads` (io.rs:212) as the forever-pin. Also update the module doc at io.rs:1-3 which mentions `.json.gz`.
- [ ] Rewrite `crates/retune-core/src/browse.rs:50-53`: it claims Facets carries "counts for the 'All (N …)' header rows" — the struct (55-59) holds only three Vec<String>; counts live in the shell (library_commands.rs:44). Describe only the three filtered, sorted, deduplicated facet lists.

## Commit 13 — `docs: remove completed yagni-solid worklist`

- [ ] Delete `docs/yagni-solid.md`.

---

## Rejected / dropped (do not re-discover)

- **backfill_metadata is retirable** — false premise: album/episode/chapter tracks still sync with `added_at: None`, so the backfill is live recurring behavior, not one-time migration.
- **Shared fixture() helper across retune-audio test files** — common-module wiring costs as many lines as it saves; per-file fixture helpers are the idiomatic Rust integration-test pattern.
- **Split lib.rs views module** — 0 LOC delta, glob-import move with no encapsulation gain; interleaved mutating/IO code makes the seam dirtier than claimed.
- **Shared disposition classifier for snapshot_page / GenreSource::artist** — the two matches diverge behaviorally (flag targets, log-once semantics, Json arm); realistic saving ~10 lines for an enum + coupling. (Its one salvageable piece — lib.rs persist_cooldown — is in Commit 7.)
- **Consolidate shift/cmd/plain click-selection three ways** — no actual behavioral divergence exists (all three agree); the generic `nextSelection<T>` plus Set↔array adapters is exactly the parameterized DRY the owner rejects.
- **Merge the two atomic_write implementations** — the differences are the token store's deliberate security posture; consolidating couples app persistence to the Spotify auth crate for ~15 lines.
- (No surviving findings were dropped during synthesis; three pairs of overlapping findings were merged: keychain migration ×3 → Commit 3, playlist persistence ×2 → Commit 6, outage-handling/artist_albums cooldown ×2 → Commit 7, local_spike ×2 → Commit 1, test scaffolding ×3 → Commit 11.)
